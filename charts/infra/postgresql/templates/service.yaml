apiVersion: v1
kind: Service
metadata:
  name: postgresql
spec:
  type: {{ .Values.service.type }}
  selector:
    app: postgresql
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.container.port }}
      protocol: TCP
